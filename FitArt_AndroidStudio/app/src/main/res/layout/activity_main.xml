<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout
xmlns:android="http://schemas.android.com/apk/res/android"
xmlns:map="http://schemas.android.com/apk/res-auto"
xmlns:tools="http://schemas.android.com/tools"
android:layout_height="match_parent"
android:layout_width="match_parent">

    <ImageView
        android:id="@+id/distance_color_block"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentTop="true"
        android:layout_alignParentBottom="true"
        map:srcCompat="@color/colorPrimary" />

    <include
        android:id="@+id/toolbar"
        layout="@layout/toolbar"
        map:titleTextColor="@color/colorWhite" />


    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/button_start"
        android:layout_below="@+id/toolbar"
        android:layout_margin="8dp"
        android:background="@color/colorWhite"
        android:padding="4dp"
        android:scrollbars="vertical">

    </androidx.recyclerview.widget.RecyclerView>

    <Button
        android:id="@+id/button_start"
        android:layout_width="120dp"
        android:layout_height="58dp"
        android:layout_alignTop="@+id/action_bar_background"
        android:layout_alignParentBottom="true"
        android:layout_margin="16dp"
        android:layout_toStartOf="@+id/button_archive"
        android:layout_toEndOf="@+id/button_draw"
        android:background="@drawable/button_background02"
        android:text="Start"
        android:textColor="@color/colorWhite" />

    <Button
        android:id="@+id/button_draw"
        android:layout_width="120dp"
        android:layout_height="39dp"
        android:layout_alignTop="@+id/action_bar_background"
        android:layout_alignParentStart="true"
        android:layout_alignParentBottom="true"
        android:layout_marginTop="22dp"
        android:layout_marginStart="19dp"
        android:layout_marginBottom="28dp"
        android:background="@drawable/button_background01"
        android:text="Draw"
        android:textColor="@color/colorWhite" />

    <Button
        android:id="@+id/button_archive"
        android:layout_width="118dp"
        android:layout_height="39dp"
        android:layout_below="@+id/recycler_view"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginTop="22dp"
        android:layout_marginEnd="19dp"
        android:layout_marginBottom="28dp"
        android:background="@drawable/button_background01"
        android:text="Gallery"
        android:textColor="@color/colorWhite" />

</RelativeLayout>